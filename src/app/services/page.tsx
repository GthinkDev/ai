"use client";

import type { FC } from "react";

import React, { memo } from "react";

const ServicesPage: FC = () => {
  return <div className={" container  "}>ServicesPage</div>;
};

export default memo(ServicesPage);
