import type { FC } from "react";

import React, { memo } from "react";

const WorksPage: FC = () => {
  return (
    <div>
      <h1>This is the Works</h1>
    </div>
  );
};

export default memo(WorksPage);
